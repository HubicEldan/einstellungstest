import { NodesToArrayPipe } from './nodes-to-array.pipe';

describe('NodesToArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new NodesToArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
